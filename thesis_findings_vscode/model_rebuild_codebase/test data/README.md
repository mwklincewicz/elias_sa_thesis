# Stories Test Data Folder

This folder intentionally excludes generated Stories CSV files from the public repository.

Generate the local Stories CSV with:

```powershell
python run_models.py prepare-data
```
